apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hupi.fullname" . }}-providers
  labels:
    {{- include "hupi.labels" . | nindent 4 }}
data:
  providers.yaml: |
{{ .Values.providers.config | indent 4 }}
