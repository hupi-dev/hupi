# ClusterIP only, deliberately no Ingress — see 10-admin-ui-deployment.yaml.
apiVersion: v1
kind: Service
metadata:
  name: hupi-admin-ui
spec:
  selector:
    app: hupi-admin-ui
  ports:
    - name: http
      port: 80
      targetPort: http
